
 apply the app.js functionality 
 create the mongoose, session in app 
  create the product with mongoose
  connect the product with controllers
  creat the user 
  connect a demo user in database(Atlas)
  add the user in app js 
  add the at the cart method to the user 
  connect the add to cart 
  fix the ejs 
  add the ejs (bootstrap here)

  For Details : 
  add the route based the products Id ( params) 
  fix the controller( get the params,find the product , send the details to the front)

  For Orders: 
  fix the UI 
  add the route 
  add cotroller, Order the product show it in order page(create schema load it,), fix the delete method on the user, delete it from the card , 


Deleting item from card : 
create root 
get the id
use the delete function from user schema 

Invoice functionality : 
add the link
create the route with proms functionality 
get the id from url
check the orders
creat the controller 
use pdfKit (documentation) 
add the download invoice feature => use the download true querry feature like edit one 


Admin Products page:-
delete functionality -> outside js inside public folder 
root with delete functionality from node 
controller added, 
add util ( useful in edit product too)

edit functionality -> work with link base  fix the routes , update the ejs 

authontication -> update the navbar, create the route seperate file 

signup : add the validation , install the bcrypt error sucrity, hundle the bcrypt, send email with email sender SMTP , validate the sign up given data from user in get with node-validation , create the error message box in ejs, be carefull when setting the user (base6)

login : write the roots , controller main function , find the user by email , handle the false condition , becrypt the password matching , rand the pages,
create the authontication gard middleware  

Signout : destroy the session (bugg in creating the session from login again fix)


csrf token: app.js use csrf,fix UI, than controllers (bug ? take a look to the sighn up : ignore )

to do : Admin panel :)

