<%- include('../layouts/start.ejs') %>
     <!-- <link rel="stylesheet" href="/Css/addProduct.css"> -->
      <link href="/bootstrap/dist/css/bootstrap.min.css" rel="stylesheet">
      <script defer src="/bootstrap/dist/js/bootstrap.bundle.min.js"></script>
      <link rel="stylesheet" href="/Css/responsive.css">
<script src="/js/main.js" defer></script>
</head>

<body>
     <div class="Container">
          <%- include('../layouts/navigation.ejs') %>
           <main class="flex_1">
           <% if(prods.length > 0) { %>
               <ul class="card_continer">

                    <% prods.forEach(prod=> { %>
                           <% if (prod.productId) { %>
                         <li class="card_item">
                         <!-- <div class="card text-bg-dark">
                              <img src="<%= prod.productId.imageUrl %>" class="card-img" alt="<%= prod.productId.title %>">
                              <div class="card-img-overlay">
                                   <h5 class="card-title text-black text-center"><%= prod.productId.title %></h5>
                                   <p class="card-text text-black text-center"> <%= prod.productId.description %></p>
                                   <p class="card-text text-black text-center"><small>You have <%= prod.quantity %> of this car in your card..</small></p>
                                   <div class="flex ">
                                   <form action="/cart-delete-item" method="POST" class="flex_1">
                                        <input type="hidden" value="<%= prod.productId._id %>" name="productId">
                                         <input type="hidden" name="_csrf" value="<%=csrfToken%>">
                                        <button class="btn danger btn-outline-light" type="submit">Delete</button>
                                   </form>
                                   <form action="/order_Item" method="POST" class="flex_1">
                                        <input type="hidden" value="<%= prod.productId._id %>" name="productId">
                                         <input type="hidden" name="_csrf" value="<%=csrfToken%>">
                                        <button class="btn danger btn-outline-light m-1" type="submit">Order</button>
                                   </form>
                                   </div>
                              </div> -->
                              <div class="card custom-card">
                                        <img src="<%= prod.productId.imageUrl %>" class="card-img-top" alt="<%= prod.productId.title %>">

                                        <div class="card-body text-center">
                                        <h5 class="card-title"><%= prod.productId.title %></h5>
                                        <p class="card-text desc"><%= prod.productId.description %></p>
                                        <p class="card-text small-text">
                                             You have <%= prod.quantity %> in your cart
                                        </p>

                                        <div class="flex">
                                             <form action="/cart-delete-item" method="POST" class="flex_1">
                                             <input type="hidden" value="<%= prod.productId._id %>" name="productId">
                                             <input type="hidden" name="_csrf" value="<%=csrfToken%>">
                                             <button class="btn custom-btn delete" type="submit">Delete</button>
                                             </form>

                                             <form action="/order_Item" method="POST" class="flex_1">
                                             <input type="hidden" value="<%= prod.productId._id %>" name="productId">
                                             <input type="hidden" name="_csrf" value="<%=csrfToken%>">
                                             <button class="btn custom-btn m-1" type="submit">Order</button>
                                             </form>
                                        </div>
                                        </div>
                              </div>
                              </li>
                              <% } %>
                              <% }) %>
                         </ul>
          <% } else { %>
          <h1>No Product Yet!</h1>
        <% } %>
        
</div>
          <%- include('../layouts/end.ejs') %>